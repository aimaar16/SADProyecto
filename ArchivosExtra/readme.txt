En esta carpeta se encuentran los archivos para extraer las reviews y filtrarlas por Canadá, aunque puede utilizarse para filtrar más cosas. 
Para ejecutralos procurar tener el archivo en la misma carpeta que el script y comprobar que el nombre en el código coincide con el del archivo.
